<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.ybu.animalfinder">

    <queries>
        <intent>
            <action android:name="android.intent.action.TTS_SERVICE" />
        </intent>
    </queries>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AnimalFinder">
        <activity android:name=".ui.ScoreBoard"></activity>
        <activity android:name=".ui.Level5" />
        <activity android:name=".ui.Level4" />
        <activity android:name=".ui.Level3" />
        <activity android:name=".ui.Level2" />
        <activity android:name=".ui.Level1" />
        <activity android:name=".ui.PlayGame" />
        <activity android:name=".ui.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>